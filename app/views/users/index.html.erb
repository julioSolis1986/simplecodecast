<div id="wrapper-conteudo">
  <div class='page-header'>
    <h1><%= resource_class.model_name.human.pluralize %></h1>
    <h2><i class='icon-book'></i> Listagem</h2>
  </div>

  <div class='table-header'>
    <h1>
      <i class='icon-tag'></i>
      <a class='ativo' href='#'>Todos</a>
    </h1>
    <h1>
      <a href='#'>Ativos</a>
    </h1>
    <h1>
      <a href='#'>Inativos</a>
    </h1>

    <%= render 'shared/search' %>
  </div>

  <table class="lista-conteudo">
    <thead>
    <% %w(name email).each do |attr| %>
        <th><%= ordenacao(resource_class.human_attribute_name attr) %></th>
    <% end %>
      <th></th>
    </thead>

  <% @users.each do |user| %>
    <%= render user %>
  <% end %>
  </table>

  <br />

  <div id="conteudo-rodape">
  <%= link_to 'Novo Usuário', new_user_path, :class => 'btn btn-primary' %>
  <%= pagination @users %>
  </div>
</div>

<div id="dialog-modal"></div>